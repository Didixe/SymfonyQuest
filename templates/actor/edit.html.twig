{% extends 'base.html.twig' %}

{% block title %}Edit Actor{% endblock %}

{% block body %}
    <div class="page de connection">
        <h1 class="mb-3">Modifier</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="oneUnderTheOther">
            <button class="btn mt-3 btn-primary text-white">{{ button_label|default('Sauvegarder') }}</button>
            <form method="post" action="{{ path('app_actor_delete', {'id': actor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.id) }}">
                <button class="btn mt-3 btn-primary text-white">Effacer</button>
            </form>
        </div>
        {{ form_end(form) }}


        <a class="mt-3" href="{{ path('app_actor') }}">retour à la liste des acteurs</a>
    </div>

{% endblock %}