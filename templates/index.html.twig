{# <!doctype html>
<html lang="fr">
<head>
   <meta charset="UTF-8">
   <title>Wild Series</title>
</head>
<body>
   <h1>{{ message }}</h1>
</body>
</html> #}
{% extends 'base.html.twig' %}

{% block title %}Wild Series{% endblock %}

{% block body %}
<h1>{{ message }}</h1>

<img src="{{ asset('build/images/citrouille.png') }}" alt="citrouille">
{# <img src="{{ asset('build/images/subdir/citrouille.png') }}" alt="citrouille"> #}
{% endblock %}